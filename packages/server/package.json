{
  "name": "desktop-mcp-server",
  "version": "1.0.0",
  "description": "WebRTC-based MCP server for remote desktop control — screen capture, mouse/keyboard input, shell execution, and more",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "desktop-mcp-server": "dist/index.js"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jagjerez-org/desktop-mcp-server.git",
    "directory": "packages/server"
  },
  "keywords": ["mcp", "desktop", "webrtc", "remote-control", "screen-capture", "model-context-protocol", "ai-tools"],
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "desktop-mcp-shared": "^1.0.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "sharp": "^0.34.5",
    "uuid": "^9.0.0",
    "ws": "^8.14.0"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.0",
    "@types/ws": "^8.5.0",
    "nodemon": "^3.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
