{
  "name": "@desktop-mcp/server",
  "version": "1.0.0",
  "description": "WebRTC-based MCP server for desktop control",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "desktop-mcp-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@desktop-mcp/shared": "file:../shared",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "ws": "^8.14.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.0",
    "@types/uuid": "^9.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "nodemon": "^3.0.0"
  }
}