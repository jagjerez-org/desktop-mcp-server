{
  "name": "desktop-mcp-agent",
  "version": "1.0.0",
  "description": "Desktop agent for remote control via WebRTC — pairs with desktop-mcp-server for AI-driven desktop automation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "desktop-mcp-agent": "dist/index.js"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jagjerez-org/desktop-mcp-server.git",
    "directory": "packages/agent"
  },
  "keywords": ["mcp", "desktop", "agent", "webrtc", "remote-control", "screen-capture", "automation"],
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "desktop-mcp-shared": "^1.0.0",
    "@nut-tree-fork/nut-js": "^4.2.6",
    "clipboardy": "^5.3.0",
    "commander": "^11.0.0",
    "screenshot-desktop": "^1.15.3",
    "uuid": "^9.0.0",
    "ws": "^8.14.0"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.0",
    "@types/ws": "^8.5.0",
    "nodemon": "^3.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
